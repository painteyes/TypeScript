/* Import file */
/**
 * Questo file contiene una serie di funzioni utili per la nostra applicazione.
 * L'obiettivo di separare queste funzioni in un file distinto è quello di rendere il nostro codice più organizzato e gestibile.
 * In questo modo, se dobbiamo fare modifiche o correzioni, sappiamo esattamente dove cercare.
*/
// Funzione per sommare due numeri.
export function multiplication(x, y = 1) {
    return x * y;
}
// Funzione per sottrarre due numeri.
export function division(x, y = 1) {
    return x / y;
}
